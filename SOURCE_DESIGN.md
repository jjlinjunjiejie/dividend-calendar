# 股息数据备用源系统

## 目标与数据流

每个持仓独立执行“抓取 → 解析 → 校验 → 选择来源”。HTTP 成功不代表抓取成功；空 JSON、错误页面、字段错位、无效日期、非有限金额、重复记录、过旧数据和已知分配记录缺失都会触发切换。

- iShares 持仓：iShares 官方 API → BlackRock 官方 API → StockAnalysis → Nasdaq → DividendHistory → 有效缓存。
- QQQ / VOO：StockAnalysis → Nasdaq → DividendHistory → 有效缓存。
- 支付日程：iShares PDF → BlackRock PDF → 有效官方日程缓存 → 根据已验证历史推算。
- NAV / 收益率：iShares screener → BlackRock screener → 从已验证的过去 365 天现金分配计算预估。

iShares 与 BlackRock 是同一机构的两个访问入口，不能视为独立数据生产者。StockAnalysis、Nasdaq 与 DividendHistory 提供额外获取路径，但免费公共端点没有可用性保证。所有路径均不需要账户、API 密钥或付费服务。

## 模块职责

| 文件 | 职责 |
|---|---|
| `data_sources.py` | 网络请求、站点解析、统一记录校验、来源顺序、缓存、诊断 |
| `update_calendar.py` | 分配周期、正式/推算日期、金额计算、ICS 输出、稳定事件元数据 |
| `run_update.py` | 校验配置与 README 标记，成功生成后写入日历并同步持仓表 |
| `tests/test_calendar.py` | 原有缺陷和降级场景的无网络回归测试 |

每个来源输出标准记录：ticker、ex_date、record_date、payable_date、per_share。选源后附加 provider 和 cached 状态。持股数只在生成时参与计算，不写入行情缓存，因此调整持仓后使用缓存仍会按最新股数重新计算。

配置中的 `source` 表示基金类别及原始首选来源。切换到 Nasdaq 不会将月度 iShares 基金误变成季度基金。当前仅支持 USD、iShares 月度持仓及季度权益基金；不支持的配置会在联网前报错。

## 校验与切换规则

1. iShares 检查 productId 与 USD；StockAnalysis 检查标题 ticker，并按表头映射列；Nasdaq 检查现金分配类型和货币。DividendHistory 校验 ticker 和表头，只接受历史行，排除 unconfirmed/estimated 与页面内置预测。Nasdaq 对部分 ETF 可能返回空表，已实测 VOO 的此情况，因此增加 DividendHistory 作为进一步兜底。
2. iShares 关键列必须等长。所有解析出的分配行都需通过校验，不能静默丢弃坏行后继续发布。
3. 股息金额必须有限且非负；支付日不得早于除息日或晚于其后 120 天。已公布支付日不得超过当前日期 180 天。
4. 同一除息日与支付日组合不能重复。月度基金至少 6 条历史，季度基金至少 4 条；历史必须覆盖过去一年，且该期间不能出现超过相应新鲜度阈值的空档，避免用半年的数据冒充全年现金分配。
5. 最近支付记录不得落后超过 75 天（月度）或 150 天（季度）。已到期基金以到期年末作为新鲜度检查截止点。
6. 有有效历史快照时，当前来源不得丢弃近 365 天及已知未来的分配记录；按除息日比较，允许支付日修正。
7. 任一来源失败会记录原因并尝试下一来源；每次运行仍从主源开始，避免长期滞留备用源。

每个 HTTP 请求超时为 12 秒，每个来源只尝试一次，通过下一来源完成重试。源选择发生在解析和校验之后，解决“官方主站返回空数据时根本没有尝试备用站”的问题。

这些校验不等价于多个来源逐笔交叉核对。若来源返回格式正确、时效阈值内但事实错误的数据，系统可能无法识别；这一限制记录在此，不把校验通过描述成金额保证。

## 缓存与故障边界

- 默认目录：`.cache/dividend-calendar/`，已加入 `.gitignore`。
- `last-good.json`：版本号、持仓来源身份、成功抓取时间、标准化每股分配记录、已验证常规日程。
- `source-status.json`：本次执行时间、是否成功、每个数据类别最终来源、失败原因和缓存使用情况。
- 有效期：自最后一次在线抓取并校验成功起，最多 7 × 24 小时。缓存命中不会续期。
- 快照只在整个日历计算成功后保存。使用缓存生成时保留原抓取时间。
- GitHub Actions Cache 跨运行保存快照；诊断作为独立 Artifact 保存 7 天。缓存不进入 Git，避免定时抓取制造空提交。
- 有有效股息历史但没有官方日程时，允许推算并显式标识；没有可靠股息历史时，不用零金额替代、不擦除旧历史，而是让工作流失败。
- 首次运行或 Actions 缓存被清理时，没有本地兜底，只能依赖在线来源。

如需调整缓存有效期，可修改 `DividendSources(..., max_cache_days=7)` 的默认值；调整历史新鲜度和最少记录数需修改 `validate_rows` 并同步测试。默认阈值面向本仓库的现有八个持仓，不应直接套用于新发行、停派或其他频率基金。

## 分配周期与预估状态

依据官方 PDF 的 Bond and Equity Funds 表格，常规月度周期为 2–11 月各一次、12 月两次，1 月常规分配提前到 12 月。解析时保留表格列位置，仅提取该表的常规列；额外 excise 分配列与下方其他基金表不进入常规预估。

每笔常规分配独立使用年化预估 ÷ 12。第二笔 12 月分配映射为次年 1 月周期，第一笔实际金额不会冲减第二笔预估。额外分配只有出现正式记录才计入。已公布金额按除息周期替换预估，可处理支付日期移动。

季度基金按最近每个除息季度的支付模式推算；仅周末向前调整，不建立交易所节假日日历，所以推算支付日始终标为“推算支付日”。预估价格和收益率不可用时，直接使用历史现金分配，不虚构价格或收益率。

所有金额使用 Decimal 运算，税后因子固定为 0.90，最后一步才截断标题整数和备注一位小数。事件标题维持纯数字；详细状态写在备注中。

## 更新与验证

相同 UID 的内容完全一致时保留原 DTSTAMP、LAST-MODIFIED 和 SEQUENCE；内容改变时才更新时间并增加 SEQUENCE。输出内容不变时不重写文件。

推荐执行：

```sh
python -m pip install -r requirements.txt
python -m unittest discover -s tests -v
python run_update.py
```

测试用小型响应样本验证真实表头、字段名和支付日映射，同时注入空数据、重复记录、失效缓存和网络失败。在线烟雾测试验证当前来源可访问，但不替代无网络回归测试，也不保证未来页面结构不变。
